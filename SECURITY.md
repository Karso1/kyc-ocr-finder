# Security

This repository contains code only. Do not commit or upload KYC images, PDFs,
OCR output, identity numbers, names, credentials, or browser profiles.

Before publishing, review `git status` and confirm that only source code and
documentation are staged. The provided `.gitignore` excludes common document
and OCR-result file types, but it is not a substitute for review.
